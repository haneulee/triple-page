import { FC } from "react";
import GlobalStyle from "src/assets/styles/global-styles";

export const App: FC = () => {
	return (
		<div className="App">
			<GlobalStyle />
		</div>
	);
};

export default App;
